# Credentials only. A fresh template selects LOCAL_ONLY/NONE in discipline.md,
# so this file may remain empty and the app still works with local storage.
#
# To select Supabase or Firebase, update BACKEND_PROVIDER and AUTH_MODE in
# discipline.md, run `npm run discipline:provider:generate`, then copy the
# matching provider-specific example below and fill its credentials.
